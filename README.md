# Voltai Sales OS — releases

Public distribution for the Voltai Sales OS Mac app. Hosts the Sparkle
`appcast.xml` and the signed release zips (under Releases). The app source lives
in the private repo `voltavi/voltai-sales-os`.

Releases are published with `mac/release.sh` from the source repo.
